(function () {
    'use static';

       // start app
    var app = angular.module('app', [
        'ngRoute', // app route
        'ngSanitize'
    ]);

    app.run(['$route', function ($route) {
        // 
    }]);

})();